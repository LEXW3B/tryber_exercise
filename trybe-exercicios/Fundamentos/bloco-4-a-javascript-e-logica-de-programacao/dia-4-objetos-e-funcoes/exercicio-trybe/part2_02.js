function returnsGreaterNumber(arr){
    for(let i=0; i<=arr.length; i+=1){
        //console.log(arr.length);
    }
    console.log(arr.length-2);

    // let big = arr.reduce(function(a,b){
    //     return Math.max(a,b);
    // },-Infinity);
    // console.log(big[i]);
}
returnsGreaterNumber([2,3,6,7,10,1]);
