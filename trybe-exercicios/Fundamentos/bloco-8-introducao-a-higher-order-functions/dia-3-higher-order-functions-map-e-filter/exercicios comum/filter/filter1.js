const arr = ['Ana', 'Pedro', 'Giovana'];

// arr.filter((element, index, array) => console.log('Elemento: ', element, 'Index: ', index, 'Array: ', array));
const filtered = arr.filter((element) => element === 'Ana');
console.log(filtered);
